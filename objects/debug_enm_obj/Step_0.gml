/// @description Insert description here
// You can write your code in this editor

skeletonUpdate(skel, x, y);

if (keyboard_check_pressed(vk_alt)) {
	skeletonPlayAnim(skel, global.debug_anims.default_pose);
}